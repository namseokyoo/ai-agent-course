---
title: "전체화면 슬라이드쇼"
publish: true
---

<div id="course-slideshow-root" class="course-slideshow-root">
  <div class="slideshow-panel">
    <div class="slideshow-panel-header">
      <div>
        <div class="slideshow-kicker">Presentation Mode</div>
        <strong>강의 선택 후 바로 발표하세요</strong>
      </div>
      <span>← / → 이동 · F 전체화면</span>
    </div>
    <div class="course-toolbar">
      <label for="lectureSelect">강의</label>
      <select id="lectureSelect" aria-label="강의 선택"></select>
      <button id="fullscreenBtn" type="button">전체화면 시작</button>
    </div>
  </div>

  <div id="slideStage" class="slide-stage" tabindex="0" aria-label="슬라이드쇼 화면">
    <button id="prevBtn" class="nav-btn nav-prev" type="button" aria-label="이전 슬라이드">‹</button>
    <img id="slideImage" alt="강의 슬라이드" />
    <button id="nextBtn" class="nav-btn nav-next" type="button" aria-label="다음 슬라이드">›</button>
  </div>

  <div id="slideCaption" class="slide-caption"></div>
</div>

<style>
.course-slideshow-root {
  margin-top: .3rem;
}

.slideshow-panel {
  margin: 0 0 1rem;
  padding: 1rem;
  border-radius: 22px;
  background: rgba(255, 255, 255, 0.82);
  border: 1px solid rgba(75, 183, 244, 0.22);
  box-shadow: 0 12px 28px rgba(16, 35, 63, 0.08);
}

.slideshow-panel-header {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 1rem;
  margin-bottom: .85rem;
  color: #10233f;
}

.slideshow-panel-header strong {
  display: block;
  font-size: 1.05rem;
  line-height: 1.25;
}

.slideshow-panel-header span {
  color: #6c7b91;
  font-size: .82rem;
  white-space: nowrap;
}

.slideshow-kicker {
  margin-bottom: .2rem;
  color: #1489bd;
  font-size: .76rem;
  font-weight: 900;
  letter-spacing: .08em;
  text-transform: uppercase;
}

.course-toolbar {
  display: grid;
  grid-template-columns: auto minmax(220px, 1fr) auto;
  gap: .65rem;
  align-items: center;
}

.course-toolbar label {
  color: #2f4058;
  font-size: .9rem;
  font-weight: 900;
}

.course-toolbar select,
.course-toolbar button {
  min-height: 44px;
  border: 1px solid rgba(16, 35, 63, 0.16);
  border-radius: 14px;
  padding: .62rem .86rem;
  background: white;
  color: #10233f;
  font-weight: 800;
  cursor: pointer;
}

.course-toolbar button {
  background: linear-gradient(135deg, #dff7ff, #e8fff8);
  color: #123047;
  min-width: 140px;
}

.slide-stage {
  position: relative;
  width: 100%;
  aspect-ratio: 3 / 2;
  background: #07111f;
  border-radius: 18px;
  overflow: hidden;
  box-shadow: 0 14px 36px rgba(0,0,0,.18);
  display: flex;
  align-items: center;
  justify-content: center;
  touch-action: pan-y;
  user-select: none;
}

.slide-stage:fullscreen {
  width: 100vw;
  height: 100vh;
  aspect-ratio: auto;
  border-radius: 0;
  cursor: none;
}

.slide-stage:fullscreen .nav-btn {
  display: none;
}

.slide-stage img {
  max-width: 100%;
  max-height: 100%;
  width: 100%;
  height: 100%;
  object-fit: contain;
  background: #07111f;
}

.nav-btn {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  z-index: 2;
  width: 46px;
  height: 66px;
  border: 0;
  border-radius: 14px;
  cursor: pointer;
  background: rgba(255,255,255,.72);
  color: #123047;
  font-size: 2.5rem;
  line-height: 1;
}

.nav-prev { left: 12px; }
.nav-next { right: 12px; }

.slide-caption {
  margin: .5rem auto 0;
  width: fit-content;
  max-width: 92%;
  background: rgba(16, 35, 63, .06);
  color: #44546a;
  padding: .25rem .62rem;
  border-radius: 999px;
  font-weight: 700;
  font-size: .76rem;
  line-height: 1.35;
  text-align: center;
}

@media (max-width: 720px) {
  .slideshow-panel { padding: .9rem; }
  .slideshow-panel-header { display: block; }
  .slideshow-panel-header span { display: block; margin-top: .35rem; white-space: normal; }
  .course-toolbar { grid-template-columns: 1fr; }
  .course-toolbar select,
  .course-toolbar button { width: 100%; }
  .nav-btn { width: 36px; height: 54px; font-size: 2.1rem; border-radius: 12px; }
  .nav-prev { left: 8px; }
  .nav-next { right: 8px; }
  .slide-caption { font-size: .68rem; padding: .2rem .5rem; }
}
</style>
